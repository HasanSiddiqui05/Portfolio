"use client"
import { useEffect, useRef } from "react";
import gsap from "gsap";

gsap.registerPlugin(ScrollTrigger)
const header = document.querySelector("header");

export default function FlowingDivs() {
  return (
    <div>

    </div>
  )
}
